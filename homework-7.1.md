<? php


import time
print "1. Text only"
print "2. Logo only"
print "3. Both text and logo"
choice = int(input("choose (1 or 2 or 3) : "))
path = "images/"
logo_path = "logo/"
paths = "water_marked_images"
if not os.path.exists(paths):
    os.makedirs('water_marked_images')
    

// This funtion test if various text, logo, and both will execute when prompted     
    
// The Variables that work with in the scope of this function are  int(input) 

// The return value of each function are (print "1. Text only") (print "2. Logo only") (print "3. Both text and logo")


    
?>